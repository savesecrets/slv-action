{
  "name": "slv-action",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "keywords": [
    "actions",
    "slv",
    "savesecrets",
    "cli",
    "secret-manager",
    "setup"
  ],
  "homepage": "https://github.com/savesecrets/slv-action",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/savesecrets/slv-action.git"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/tool-cache": "^2.0.1",
    "@octokit/auth-token": "^5.0.0",
    "@octokit/rest": "^20.0.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.56.0",
    "jest": "^29.7.0"
  }
}
